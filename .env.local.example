# 复制本文件为 .env.local 并填写实际值（.env.local 不会被提交到 git）
# 用于本地调试「Submit to teacher」

# Resend API Key（在 https://resend.com → API Keys 里创建，以 re_ 开头）
RESEND_API_KEY=re_xxxxxxxxxxxx

# 接收作业的邮箱
TEACHER_EMAIL=your-email@example.com

# 前端提交时请求的地址（本地调试填下面这行）
VITE_SUBMIT_API_URL=http://localhost:3001/submit
